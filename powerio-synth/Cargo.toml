[package]
name = "powerio-synth"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Deterministic synthetic power-system case generation for PowerIO."
readme = "README.md"
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/powerio-synth"
keywords = ["power-system", "synthetic", "matpower", "testing"]
categories = ["science", "simulation"]

[dependencies]
powerio.workspace = true
rand = { version = "0.9", default-features = false, features = ["std"] }
rand_chacha = { version = "0.9", default-features = false, features = ["std"] }
serde.workspace = true

[lints]
workspace = true
